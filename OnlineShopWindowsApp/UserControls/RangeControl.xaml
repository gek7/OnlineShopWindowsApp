<UserControl x:Class="OnlineShopWindowsApp.UserControls.RangeControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:OnlineShopWindowsApp.UserControls"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="350" d:DesignWidth="300">
    <Grid>
        <GroupBox Header="{Binding Header}" FontSize="12">
            <StackPanel>
                <TextBlock Text="От" FontSize="10"  />
                <TextBox  materialDesign:TextFieldAssist.SuffixText="{Binding Suffix}" materialDesign:TextFieldAssist.PrefixText="{Binding Prefix}" PreviewTextInput="TextBox_PreviewTextInput"/>
                <TextBlock Text="До" FontSize="10"/>
                <TextBox materialDesign:TextFieldAssist.SuffixText="{Binding Suffix}" materialDesign:TextFieldAssist.PrefixText="{Binding Prefix}" PreviewTextInput="TextBox_PreviewTextInput"/>
            </StackPanel>
        </GroupBox>
    </Grid>
</UserControl>
